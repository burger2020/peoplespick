<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/bottom_menu_home"
        app:showAsAction="always"
        android:icon="@mipmap/ic_bottom_home"
        android:title="홈" />
    <item
        android:id="@+id/bottom_menu_search"
        app:showAsAction="always"
        android:icon="@mipmap/ic_bottom_search"
        android:title="검색" />
    <item
        android:id="@+id/bottom_menu_new"
        app:showAsAction="always"
        android:icon="@mipmap/ic_bottom_edit"
        android:title="글 쓰기" />
    <item
        android:id="@+id/bottom_menu_alarm"
        app:showAsAction="always"
        android:icon="@mipmap/ic_bottom_alarm"
        android:title="알림"/>
</menu>